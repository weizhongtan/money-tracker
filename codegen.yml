---
schema:
  - http://localhost:3000/v1/graphql
documents:
  - './src/**/*.gql'
overwrite: true
generates:
  './src/generated/graphql.tsx':
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      skipTypename: false
      withHooks: true
      withHOC: false
      withComponent: false
      scalars:
        timestamptz: string
        uuid: string
        numeric: number
  './graphql.schema.json':
    plugins:
      - introspection
