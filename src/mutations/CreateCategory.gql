mutation CreateCategory($name: String!, $type: String) {
  insert_category(objects: { name: $name, type: $type }) {
    affected_rows
    returning {
      id
      key: id
      name
      type
    }
  }
}
